const { Connection, clusterApiUrl } = require("@solana/web3.js");

const main = async () => {


}

const runMain = async () => {
    try {
        await main();
    } catch (error) {
        console.error(error);
    }
}

runMain();